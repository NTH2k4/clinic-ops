import { useMemo, useState } from "react";
import { StatusBadge } from "../../components/StatusBadge";
import { formatTime, toDateInputValue } from "../../lib/dateTime";
import { mockStore } from "../../mocks/mockStore";
import type { AppointmentStatus } from "../../types/models";

const OPERATIONS_TODAY = "2026-08-25";
const statuses: Array<{ value: "" | AppointmentStatus; label: string }> = [{ value: "", label: "Tất cả trạng thái" }, { value: "confirmed", label: "Đã xác nhận" }, { value: "checked_in", label: "Đã check-in" }, { value: "in_progress", label: "Đang khám" }, { value: "completed", label: "Hoàn tất" }, { value: "cancelled", label: "Đã hủy" }, { value: "no_show", label: "Không đến" }];

export function OperationsCalendar() {
  const [date, setDate] = useState(OPERATIONS_TODAY);
  const [doctorId, setDoctorId] = useState("");
  const [specialtyId, setSpecialtyId] = useState("");
  const [status, setStatus] = useState<"" | AppointmentStatus>("");
  const appointments = useMemo(() => mockStore.appointments.filter((appointment) => toDateInputValue(appointment.startAt) === date && (!doctorId || appointment.doctorId === doctorId) && (!specialtyId || mockStore.doctors.find((doctor) => doctor.id === appointment.doctorId)?.specialtyId === specialtyId) && (!status || appointment.status === status)).sort((left, right) => left.startAt.localeCompare(right.startAt)), [date, doctorId, specialtyId, status]);

  return (
    <section className="mx-auto max-w-6xl"><p className="text-sm font-medium text-primary">Điều phối lịch</p><h1 className="mt-1 text-2xl font-semibold text-text">Lịch hoạt động</h1><div className="mt-5 grid gap-3 rounded-lg border border-border bg-surface p-4 shadow-panel sm:grid-cols-2 xl:grid-cols-4"><label className="text-sm font-medium text-text">Ngày<input className="mt-1 h-10 w-full rounded-md border border-border bg-surface px-3" onChange={(event) => setDate(event.target.value)} type="date" value={date} /></label><label className="text-sm font-medium text-text">Bác sĩ<select className="mt-1 h-10 w-full rounded-md border border-border bg-surface px-3" onChange={(event) => setDoctorId(event.target.value)} value={doctorId}><option value="">Tất cả bác sĩ</option>{mockStore.doctors.map((doctor) => <option key={doctor.id} value={doctor.id}>{doctor.fullName}</option>)}</select></label><label className="text-sm font-medium text-text">Chuyên khoa<select className="mt-1 h-10 w-full rounded-md border border-border bg-surface px-3" onChange={(event) => setSpecialtyId(event.target.value)} value={specialtyId}><option value="">Tất cả chuyên khoa</option>{mockStore.specialties.map((specialty) => <option key={specialty.id} value={specialty.id}>{specialty.name}</option>)}</select></label><label className="text-sm font-medium text-text">Trạng thái<select className="mt-1 h-10 w-full rounded-md border border-border bg-surface px-3" onChange={(event) => setStatus(event.target.value as "" | AppointmentStatus)} value={status}>{statuses.map((item) => <option key={item.value || "all"} value={item.value}>{item.label}</option>)}</select></label></div><div className="mt-6 overflow-x-auto rounded-lg border border-border bg-surface shadow-panel"><table className="hidden min-w-full text-left text-sm md:table"><thead className="bg-surface-muted text-text-muted"><tr><th className="p-3 font-medium">Giờ</th><th className="p-3 font-medium">Bệnh nhân</th><th className="p-3 font-medium">Bác sĩ</th><th className="p-3 font-medium">Dịch vụ</th><th className="p-3 font-medium">Trạng thái</th></tr></thead><tbody>{appointments.map((appointment) => { const patient = mockStore.patients.find((candidate) => candidate.id === appointment.patientId); const doctor = mockStore.doctors.find((candidate) => candidate.id === appointment.doctorId); const service = mockStore.services.find((candidate) => candidate.id === appointment.serviceId); return <tr className="border-t border-border" key={appointment.id}><td className="p-3 font-semibold text-primary">{formatTime(appointment.startAt)}</td><td className="p-3 font-medium text-text">{patient?.fullName}</td><td className="p-3 text-text">{doctor?.fullName}</td><td className="p-3 text-text-muted">{service?.name}</td><td className="p-3"><StatusBadge status={appointment.status} /></td></tr>; })}</tbody></table><ul className="divide-y divide-border md:hidden">{appointments.map((appointment) => { const patient = mockStore.patients.find((candidate) => candidate.id === appointment.patientId); const doctor = mockStore.doctors.find((candidate) => candidate.id === appointment.doctorId); return <li className="flex gap-3 p-3" key={appointment.id}><span className="w-12 shrink-0 font-semibold text-primary">{formatTime(appointment.startAt)}</span><div className="min-w-0 flex-1"><p className="font-medium text-text">{patient?.fullName}</p><p className="mt-1 text-sm text-text-muted">{doctor?.fullName}</p><div className="mt-2"><StatusBadge status={appointment.status} /></div></div></li>; })}</ul></div>{!appointments.length ? <p className="mt-4 text-sm text-text-muted">Không có lịch hẹn phù hợp.</p> : null}</section>
  );
}
